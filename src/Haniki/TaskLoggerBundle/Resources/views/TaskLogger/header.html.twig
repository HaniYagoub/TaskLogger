<nav class="navbar navbar-inverse navbar-static-top" role="navigation">
    <div class="container">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
          <span class="sr-only">Toggle navigation</span>
        </button>
        <a class="navbar-brand" href="{{ path('show_tasks') }}">TaskLogger</a>
      </div>
      <div id="navbar" class="navbar-collapse collapse">
        <ul class="nav navbar-nav navbar-right">
          <li><a href="{{ path('show_tasks', {date: date.modify('-1 day').format('Y-m-d')}) }}" class="glyphicon glyphicon-chevron-left"></a></li>
          <li><a id="log-date">{{date.modify('+1 day').format('Y-m-d')}}</a></li>
          <li><a href="{{ path('show_tasks', {date: date.modify('+1 day').format('Y-m-d')}) }}" class="glyphicon glyphicon-chevron-right"></a></li>
        </ul>
      </div>
    </div>
  </nav>